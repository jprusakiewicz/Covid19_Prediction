import pandas as pd
import json


def read_data(path: str = "data/data.json") -> pd.DataFrame:
    """ load your data here. This may look something like:
        with open(path) as f:
                data = json.load(f)
            return pd.DataFrame(data["records"])
    """
    raise NotImplemented
